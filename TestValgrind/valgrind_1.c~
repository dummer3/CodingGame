  #include <stdlib.h>
#include <stdio.h>

int main(void)
{
  int *p = malloc(11 * sizeof *p);
  float *f, *g = malloc(14 * sizeof *f);
  int i = 0;
  if (p != NULL){
    g++ = f++;
    while (11*i-i*i+1 > 0){
      i+=2;
      p[i] = 42;
      f = g++;
    }
    i = 0;
    while (11*i-i*i+1 > 0){
      i+=2;
      printf("\t%d",p[i]);
    }
    printf("\n");
  }    
  free(p);

  return 0;    
}
